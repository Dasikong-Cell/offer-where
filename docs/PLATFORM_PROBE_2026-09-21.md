# 平台实机探针报告（店长直聘 / 鱼泡直聘 / 国聘 / 中华英才网 / 应届生求职网）

> 日期：2026-09-21　方式：**匿名态只读探针**（未登录、未投递、未点击任何投递按钮）
> 环境：各自独立调试 Chrome（店长直聘 9231 / 鱼泡 9232 / 国聘 9235 / 中华英才网 9230 / 应届生求职网 9236），经 `POST /api/browser/exec` 驱动
> 结论速览：**鱼泡直聘 ✅ 可采集 + 可投递（需登录）；国聘 ✅ 可采集（走 API）；店长直聘 ⚠️ 可采集但 Web 投递不可行；中华英才网 ✅ 可采集 + 可投递（无 URL 搜索，仅推荐列表）；应届生求职网 ✅ 可采集 + 可投递（51job 城市码 → 频道页卡片）**

---

## 一、结论表

| 平台 | 服务端渲染 | 列表页可采集 | 投递入口 | 登录要求 | 判定 |
|---|---|---|---|---|---|
| **鱼泡直聘** | ✅ 好（首页 2196 个 `<a>`） | ✅ `/zhaogong/{cityId}/` 一次拿到 **161 条**详情链接 | ✅ `发送简历` / `聊一聊` / `拨打电话` | 投递需登录（页面提示「点击登录，立即与老板沟通」） | **可采集 + 可投递** |
| **国聘** | ⚠️ 前端异步渲染（等 ~12s） | ✅ `.job-card` ×20，字段语义化 | 待登录后探（卡片为 JS 跳转，非 `<a>`） | 未探到投递按钮 | **可采集**（投递待登录后探） |
| **店长直聘** | ✅ 好（551 个 `<a>`） | ✅ `/joblist/{city}/` + `/job/{id}.html` | ❌ **PC 端登录/注册均跳 `/pc/download`（APP 下载页）** | Web 无求职者登录入口 | **可采集；Web 投递不可行** |

---

## 二、鱼泡直聘（yupao.com）—— 最值得先做

### 已探明的结构
| 项 | 实测值 |
|---|---|
| 城市入口 | 按 IP 自动跳 `/a367/`（昆明），城市 ID 在路径里 |
| 职位列表页 | `https://www.yupao.com/zhaogong/a367/` → **161 个详情链接**（`a[href*="/zhaogong/"][href$=".html"]`） |
| 列表项容器 | `class="acdif afih"`（混淆命名，但**按这组 class 稳定命中**，9 条/屏） |
| 详情页 | `https://www.yupao.com/zhaogong/{数字id}.html`（如 452830781）—— 可**匿名访问** |
| 详情页标题 | `h1` = 职位名 |
| 投递入口 | **`发送简历` / `聊一聊` / `拨打电话`**（三个按钮均在详情页） |
| 登录闸门 | 页面有「点击登录，立即与老板沟通」→ **投递动作必须登录** |
| JD 结构 | 招聘要求 / 招聘人数 / 薪资报酬 / 招聘有效期 / 工作地点 / 核心工作内容 / 岗位基本条件 + HR（姓名·职位·回复率）+ 工商信息（公司名/法人/成立日期） |

### 已知未解决
- **搜索参数名不是 `keyword`**：`/zhaogong/a367/?keyword=xxx` 被忽略（返回默认列表）。搜索框无 `<form>`（JS 提交）。→ 接入前需再探真实参数（试 `?key=` / `?kw=` / 或抓 XHR）。
- 薪资/公司无稳定 class（试 `[class*=salary]` 等均未命中）→ 需按详情页实际 class 精确校准（可用 JD 正文整段入库，字段留空亦可用）。
- 详情页含高德地图（`amap-marker-content` 命中我的宽泛选择器），说明**要避开 amap 相关 class**。

### 接入建议
1. 采集：列表页取 `a[href*="/zhaogong/"][href$=".html"]`（去重后可达 161 条/城市），详情页取 `h1` + 正文。
2. 投递：详情页点 `聊一聊`（或 `发送简历`）→ 需登录；建议先做 **preview 分支**（探测到按钮即返回，不点击）。
3. 城市 ID（`a367`=昆明）应做成可配置，而不是硬编码。

---

## 三、国聘（iguopin.com）—— 有 API，可能最省事

### 已探明的结构
- 列表页 `https://www.iguopin.com/job/list`：**前端异步渲染**，`<a>` 里没有职位链接（`/job/数字` 命中 false）。
- 但渲染后的卡片 class **语义化且稳定**：

| class | 含义 | 实测 |
|---|---|---|
| `.job-card` | 职位卡片 | ×20 |
| `.job-left` / `.job-right` | 卡片左右区 | ×19 / ×20 |
| `.job-name` | 职位名 | ✅ |
| `.job-district` | 地区（如「深圳-宝安区」） | ✅ |
| `.job-title` | 含完整 `title` 属性 | ✅ |
| `.job-flags` | 标签位 | — |

- 卡片内**只有 1 个 `<a>` 指向公司页**（`/company?id=...`）→ 点卡片进详情走 JS 跳转，不是链接。
- 卡片文本字段很全，可直接结构化：
  > 财富顾问（深圳宝安海秀路证券营业部）｜「深圳-宝安区」｜8~12K｜社招｜1-3年｜本科｜国投证券股份有限公司｜国企｜5000-10000人｜金融业

### 🔑 关键发现：公开 JSON 接口
页面实际调用的接口（`performance.getEntriesByType('resource')` 采集）：

```
https://gp-api.iguopin.com/api/jobs/v1/recom-job      ← 推荐职位（列表数据来源）
https://gp-api.iguopin.com/api/base/category/v1/by-alias
https://gp-api.iguopin.com/api/base/districts/v1/tree  ← 地区树
https://gp-api.iguopin.com/api/base/channelarticle/v1/list
https://api4.iguopin.com/api/spread/list
```

**建议优先验证 `gp-api.iguopin.com` 是否可匿名直接调用** —— 若可以，采集走 JSON 比 DOM 稳定得多（平台改版不影响接口）。

#### ✅ 已验证：接口匿名可调，但 `recom-job` 不是列表接口

实测（curl 直连，带 `Referer: https://www.iguopin.com/`）：

| 请求 | 结果 |
|---|---|
| `GET /api/jobs/v1/recom-job` | `{"code":405,"msg":"错误的请求方式"}` → **需要 POST** |
| `POST`（`{}` / `page` / `page_size` / `page_num` / `pageNo` / `current` / `limit` 等 6 组参数） | 全部 `{"code":200,"msg":"OK","data":{"total":0,"page":0,"page_size":0,"list":null}}` |

**结论**：
- ✅ 该接口**无需鉴权/签名**，匿名 POST 即可调用（返回规范信封 `{code,msg,data,trace}`）。
- ❌ 但它是**「某岗位的推荐位」**（`recom-job`，大概率需要 `job_id` 之类入参），**不是列表接口** —— 无参时恒返回空。
- 📌 列表页那 20 张 `.job-card` 的数据来源**尚未定位**（资源计时里只有上述辅助接口）。
  若要找真列表接口，下次需**抓真实请求体**（开 Network 域，或 hook `window.fetch`/`XMLHttpRequest`）。

**因此国聘采集的推荐路线是 DOM**：`.job-card` + `.job-name`/`.job-district`/`.job-title`（字段完整，见上表），
等 12s 让异步渲染完成即可；API 路线留作后续优化。

### 接入建议
1. 先探接口：直接 `curl https://gp-api.iguopin.com/api/jobs/v1/recom-job`，看是否需要鉴权/签名。
2. 不行再走 DOM：`.job-card` 卡片 + 点击进详情（详情页 URL 形态待确认）。
3. 国聘偏**央企国企**（与用户"软件工程/昆明"画像匹配度需评估；实测推荐位多为金融/深圳岗）。

---

## 四、店长直聘（dianzhangzhipin.com）—— 可采集，但**投递不可行**

### ⚠️ 重要更正：首访风控是一次性的
第一轮探针导航首页时被重定向到 `/safe/verify-slider?redirect=/`，页面仅 24 字
「当前账号可能存在风险，请前往验证 点击进行认证」——**当时判定为"被风控挡住"**。

**但第二轮复测时首页已能正常打开**（正文 1276 字，551 个链接，导航完整）。
说明该验证页是**首访一次性安全校验**（或已自行放行），不是永久封锁。

| 第一轮 | 第二轮（复测） |
|---|---|
| 落地 `/safe/verify-slider?redirect=/` | 落地 `/`，正常渲染 ✅ |

### 已探明的真实结构（与 BOSS 同构，因为同集团）
| 项 | 实测值 |
|---|---|
| 职位列表 | `/joblist/c156/`（`c156` 为城市段）→ 页面上有 **351 个**分类链接 |
| 分类过滤 | `/joblist/c156-p{分类id}/`（如 `p2000200`=餐饮服务、`p290202`=服务员） |
| 职位详情 | `/job/{id}.html`（如 `/job/1XV62dS9F1c~.html`）—— 与 BOSS 的 `/job_detail/xxx.html` 同构 |
| 店铺页 | `/store/{id}-{hash}.html` |
| 风控 | 详情页/列表页**匿名可读** |

### ❌ 致命问题：PC 端登录入口指向 APP 下载页
首页导航实测：

```
注册 → /pc/download
登录 → /pc/download
APP/小程序 → /pc/download
我要招人 → /pc/download
```

**求职者的注册/登录在 PC 端没有网页入口，全部跳 APP 下载页**（与易直聘同类型：APP 优先）。
→ 结论：**店长直聘可以采集（匿名），但 Web 端自动投递不可行** —— 拿不到登录态，就没有可点的投递按钮。

### 判定
| 能力 | 结论 |
|---|---|
| 采集 | ✅ 可行（`/joblist/{city}/` + `/job/{id}.html`，仅需城市段可配置） |
| 投递 | ❌ 不可行（Web 无求职者登录入口，登录/注册均跳 `/pc/download`） |
| 岗位画像 | 餐饮/零售/酒店服务业，与"软件工程/昆明/校招"匹配度低 |

**建议**：登记保留（可作采集源），但**不要投入投递实现**；若确实要投，需评估其 APP 自动化（超出当前 Web-CDP 架构）。

---

## 五、对平台的横向对比（供排期）

| 维度 | 鱼泡 | 国聘 | 店长直聘 | 中华英才网 | 应届生求职网 |
|---|---|---|---|---|---|
| 采集难度 | **低**（列表页直接有链接） | 中（异步渲染） | **低**（`/joblist/{city}/` + `/job/{id}.html`） | **低**（/`job/` 推荐列表 12 条，无 URL 搜索） | 中（列表→频道页→卡片，property JSON） |
| 投递难度 | 中（三个入口明确，需登录） | 中（需登录） | **不可行** | 中（直接投递/投简历，需登录） | 中（立即申请/先聊聊，需登录） |
| 风控强度 | 低（匿名可读详情） | 低（匿名可读） | 低（登录走 APP） | 低（匿名可读详情） | 低（匿名可读详情） |
| 岗位画像匹配度 | 蓝领/服务业，匹配度低 | 央企国企，金融/制造居多 | 店长/服务业，匹配度低 | 综合但推荐列表非技术/外地岗多 | 校招/应届，含技术岗，匹配度较高 |
| 建议 | 技术验证最省事 | 先验证 API | 仅采集源 | 接入（可用量偏低） | 接入（校招匹配度好） |

> ⚠️ 一句实话：这三家的岗位画像与用户（软件工程 / 昆明 / 校招）**匹配度都不高**——鱼泡是蓝领、店长直聘是服务业、国聘偏央企金融。
> 从"投得出有效简历"角度，优先级应低于 BOSS/智联/前程无忧/牛客。若目标是"平台覆盖广度"则按上表排期。

---

## 六、中华英才网（chinahr）/ 应届生求职网（yingjiesheng）—— 本轮新接入

### 中华英才网（chinahr.com）

| 项 | 实测值 |
|---|---|
| 列表页 | `https://www.chinahr.com/job/` 匿名可读，首页推荐 **~12 个** `/detail/{hexid}` 详情链接 |
| ⚠️ 搜索 | **Web 端无 URL 搜索**：`/search/?keyword=` 与 `/job/?keyword=` 都重定向回首页、返回相同推荐列表 |
| 详情页 | `https://www.chinahr.com/detail/{hexid}` 匿名可读，字段行清晰：`职位 薪资元/月 全职 学历 ｜经验 ｜城市｜招若干人` |
| 职位名 | `<title>` 里 `【职位招聘】`，或 `h1` |
| 公司名 | 详情页 `/company/homepage/{id}` 链接文本（最稳），兜底正文公司名正则 |
| 薪资 | 形如 `7k-10k元/月` / `面议` |
| 投递入口 | **直接投递 / 投简历 / 申请职位 / 立即投递**（点击后变「取消投递」=已投） |
| 登录态 | 未登录导航栏显示「登录｜注册」；登录后变用户名 + 退出 |

**判定**：✅ **可采集 + 可投递**，但因无 URL 搜索，列表只能是平台推荐（量小、多为非技术/外地岗），故在详情页做客户端技术岗 + 城市过滤。

### 应届生求职网（yingjiesheng.com）

> 首轮探针误判为「APP only」（首个采样详情页只有 APP 投递按钮）。复探 8 个 `/jobs/k_{id}/` 频道页后发现：频道页含多个 **Web 可投递**卡片，结论改为 **✅ 可采集 + 可投递**。

| 项 | 实测值 |
|---|---|
| 列表页 | `https://q.yingjiesheng.com/jobs/search/?jobarea=270200`（昆明=270200，51job 城市码）匿名可读，返回 ~12 个**频道页** `/jobs/k_{id}/` |
| 频道页 | `/jobs/k_{id}/` 内含多个职位卡，每张卡是 `<a href=".../jobdetail/{id}.html?property=...">` |
| property 参数 | URL 编码 JSON，含结构化字段 **jobTitle / companyName / monthSalary / jobId**（解析最稳） |
| 卡片正文 | 内嵌 城市 + 经验 + 公司 + 行业 + 薪资 + 「先聊聊/立即申请」按钮 |
| 详情页 | `https://q.yingjiesheng.com/jobdetail/{id}.html`（投递入口，Web 端「立即申请/先聊聊」） |
| 投递入口 | **立即申请 / 先聊聊 / 投简历** |
| 登录态 | 未登录点击投递可能弹登录浮层（短信/验证码/扫码登录）→ 点击后检测浮层判 `need_login` |

**判定**：✅ **可采集 + 可投递**。jobarea 已粗筛昆明，但频道内卡片可能含异地岗，采集器客户端再按城市串过滤。

---

## 七、实现结果（同日落地）

探针后立即完成接入，**四家都已进入「已接入」档**（`SUPPORTED_PLATFORMS`：6 → 10，两家在第四节判为不可行/待定仍留 `PENDING`）。

### 国聘：采集改走 API（比 DOM 好得多）★
继续深挖发现 **`/api/jobs/v1/list` 匿名 POST 可用**，且**支持 `job_name` 关键词搜索**：

| 请求 | 结果 |
|---|---|
| `{"job_name":"软件"}` | ✅ 首条 = 「软件工程师」（**服务端关键词搜索有效**） |
| `{"page":1,"page_size":20,"job_name":"Java"}` | ✅ 返回 20 条，字段 30+ |
| `keyword` / `search` / `name` 参数 | ❌ 被忽略（返回默认列表） |

单条记录字段（节选）：`job_id, job_name, company_name, min_wage, max_wage, wage_unit_cn,
education_cn, experience_cn, nature_cn, recruitment_type_cn, category_cn, district_list,
**contents（JD 正文）**, is_apply, applied, apply_instruction`

→ **采集实现**：`scripts/collect_iguopin.ts`（纯 HTTP，**秒级完成**，无 CDP 依赖）
→ **实测入库 193 条**：无公司 0 / 无城市 0 / 有真 JD(>200字) 162 条（84%）
→ **投递实现**：`server/services/apply/iguopin.ts`（详情页 `/job/detail?id={job_id}` → 点「申请职位」），需登录
→ ⚠️ **重要提醒**：193 条里**昆明岗位 = 0**（多为广东/北京/福建等），对该用户实际可用量很低

### 鱼泡直聘：采集器已就绪，但昆明无技术岗
→ **采集实现**：`scripts/collect_yupao.ts`（列表 `/zhaogong/a367/` + 详情），实测能稳定收集 33 个详情链接
→ **但技术岗过滤后 = 0**：昆明 33 个岗位**全是蓝领/工厂/服务岗**（打包工、包装工、奶茶店员、滴滴司机、
美容师、总账会计…），**零技术岗** → 0 是**正确过滤**而非 bug
→ **投递实现**：`server/services/apply/yupao.ts`（详情页 → 「聊一聊」/「发送简历」），需登录

### 中华英才网：采集器 + 投递模块（本轮新增）

→ **采集实现**：`scripts/collect_chinahr.ts`（列表 `/job/` 取 ~12 个 `/detail/` 链接 → 详情页解析字段），客户端技术岗 + 城市过滤
→ **投递实现**：`server/services/apply/chinahr.ts`（详情页 → 点「直接投递/投简历」，点击后变「取消投递」判定已投），需登录
→ ⚠️ 无 URL 搜索：推荐列表多为非技术/外地岗，昆明技术岗实际可用量低（与国聘同类型问题）
→ 注册：从 `PENDING` 移入 `SUPPORTED`（index.ts 加 `runChinahr` 导入 + `case 'chinahr'`），合约测试 E/F 仍全绿

### 应届生求职网：采集器 + 投递模块（本轮新增）

→ **采集实现**：`scripts/collect_yingjiesheng.ts`（列表 `?jobarea=270200` → 频道页 `/jobs/k_{id}/` → 职位卡，从 property JSON 取结构化字段），客户端技术岗 + 城市过滤
→ **投递实现**：`server/services/apply/yingjiesheng.ts`（单岗 `jobdetail/{id}.html` → 点「立即申请/先聊聊」，点击后弹登录浮层判 `need_login`），需登录
→ ⚠️ 首轮探针误判 APP-only，复探 8 个频道页后确认 Web 可投递
→ 注册：从 `PENDING` 移入 `SUPPORTED`（index.ts 加 `runYingjiesheng` 导入 + `case 'yingjiesheng'`），合约测试 E/F 仍全绿

### 两家的预览都已实测通过（零副作用）
```
国聘  status=preview  投递入口「申请职位」可用；本次未点击，未产生任何真实投递
鱼泡  status=preview  投递入口「发送简历」可用；本次未点击，未产生任何真实投递
```

### ⚠️ 过程中的一次事故（已修复并加测试）
给预览做实测时，**单岗接口 `/api/apply` 没有透传 `preview`** —— 本想"零副作用预览"，
结果**真的点了国聘的「申请职位」按钮**（截图确认：按钮仍显示"申请职位"、无"已投递"状态，判定未成功提交）。
已修：`/api/apply` 与 offerbiu 邮箱直投入口都补上 `preview` 透传，并新增**合约测试 F 段
「runApply 调用点必须全部透传 preview」**（该测试上线又抓出第三处遗漏）。

