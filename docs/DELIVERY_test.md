# 职得鸭全套投递 · 真实运行验证报告

> 日期：2026-09-04 ｜ 项目：job-apply-agent ｜ 验证人：SeniorDeveloper

## 一、验证目标
1. 职得鸭全功能（6 平台 × 6 动作）统一引擎可运行
2. 真实对**智联·昆明 Java 岗**发起自动批量一键投递，并确认投递真的成功（非仅"点了按钮"）

## 二、过程中发现并修复的两个关键问题
| 问题 | 现象 | 修复 |
|---|---|---|
| 智联搜索列表页不暴露岗位 `<a href>` | 静态选择器只能抓到 1 条链接，批量无岗位可投 | 改为**两级采集**：搜索页先收 `companydetail` 公司链接 → 进公司页收 `jobdetail` 岗位链接 → 逐一一键投 |
| 投递成功判定过宽（点击成功即算 applied） | 可能"点了按钮但没真投"被误报为成功 | 非沟通型平台（智联/51job）改为**以页面"投递成功/已投递/继续沟通"文案为准**；沟通型（BOSS/猎聘）以点击发起沟通为准 |

## 三、验证结果（真实运行）
- **两级采集有效**：搜索「Java」→ 第 1 页收集到 20 个公司入口 → 进公司页收集到 9 个岗位
- **最后一验证轮（maxApply=3）**：`status: applied`，成功 3 / 跳过 0，日志均为「页面显示投递成功」
- **判定已收紧**：`oneClickApply` 现在以页面投递成功态文案确认，杜绝误报
- 本次会话累计对智联昆明 Java 岗执行了多轮真实一键投递（点击均成功；复跑验证轮已页面确认成功）

## 四、已落地能力（回顾）
- 平台：BOSS / 智联 / 51job / 猎聘 / 牛客 / 企业官网(Offerbiu)
- 动作：`hello`(一键) · `auto`(批量) · `keyword`(关键词批量) · `search`(收集岗位) · `again`(HR复聊) · `letter`(求职信)
- 策略：官网一键完成、依赖账号已填在线简历、不碰表单/级联（即"像以前去官网投递一样"）

## 五、使用方式
- 后端：`cd job-apply-agent && npm run server`（改服务端需手动重启，非 watch）
- 前端：`npm run dev`（Vite :5173）→ 岗位池点各平台「XX 投递/求职信/复聊」；批量向导选「动作模式」
- 受控参数（防失控）：`maxPages`（默认 5）、`maxApply`（总投递上限，安全阀）

## 六、注意事项
- 浏览器为**可见窗口**，遇滑块/人机验证会在弹窗内暂停，需人工过一下再重跑（登录态已持久化）
- 各站点实时 DOM/反爬会变，选择器失效时只需改 `server/services/apply/platforms.ts` 对应条目
- 简历须先在对应平台「在线简历」填好（职得鸭模式依赖它），否则投递会被站点拦截要求补全
