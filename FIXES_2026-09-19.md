# 测评问题修复报告

日期：2026-09-19 ｜ 承接 `ASSESSMENT_2026-09-19.md` §五「未修问题」
提交：`7143146` → `93ed709` → `12b121e`（均已推送 GitHub + Gitee）

---

## 一、修复总览

| # | 问题 | 严重度 | 状态 | 关键结果 |
|---|---|---|---|---|
| P1-1 | BOSS 采集器抓不到公司名 → 150 条投递记录不一致 | 🔴 | ✅ 已修 | 根因实为 `collect_multi.ts` 用错选择器 |
| P1-2 | 685 个岗位缺 JD（40.6%）→ 匹配分虚高 | 🔴 | ✅ 工具+校准完成 | 无 JD 685 → **557**；JD 覆盖率 59% → **64%** |
| P2-1 | `position` 字段脏（换行/薪资残片/卡片尾巴） | 🟠 | ✅ 已修 | 含换行 59 → **0**；顺带修了 118 条 51job 卡片污染 |
| P2-2 | 依赖 9 个漏洞 | 🟠 | ✅ 已修 | 9 → **1**（仅剩不可利用的 uuid moderate） |
| P2-3 | 零自动化测试，构建坏了没人知道 | 🟠 | ✅ 已修 | selftest **51 项** + `.githooks/pre-push` |
| P2-4 | 重复岗位 / 35 条空 `apply_url` | 🟠 | ✅ 已修 | 真重复 **0**；空直链候选 **0** |
| P3 | React 开发界面 bundle 偏大 | 🟡 | ⏸ 暂缓 | 依赖升级后 2.72MB→3.91MB（生产不受影响） |

---

## 二、逐项说明

### P1-1 BOSS 公司名：根因与测评时的推断**不同**

测评时推断是「`.boss-name` 选择器失效」。实测 BOSS 列表卡片 DOM 后修正：

```
实测（2026-09-19）:
  .boss-name  => "兆富科技"      ← 好用！
  .job-name   => "兼职软件开发"   ← 好用！
  .company-name => ""            ← 不存在
  .job-title  => "兼职软件开发\n-K"  ← 含薪资，且薪资是加密字体
```

**真正的锅在 `scripts/collect_multi.ts` 的 `EXTRACT_BOSS`**：用 `.company-name` 取公司名（不存在 → 全空），
用 `.job-title` 取职位名（含薪资残片 → 脏值）。

修复：改为 `.job-name` / `.boss-name`，遍历容器从 `[class*="job-card"], li`（宽泛、易产生嵌套重复）收紧为 `.job-card-wrap`。

### 重复岗位的真正根因：`upsertJob` 去重键的 NULL 陷阱

```sql
-- 旧（错）：SQL 里 NULL = NULL 恒为 false → 历史行 company 为 NULL 时永远匹配不上 → 反复插入
WHERE source = ? AND company = ? AND position = ?
-- 新（对）
WHERE source = ? AND COALESCE(company,'') = ? AND COALESCE(position,'') = ?
```
这是「同一岗位被采多次」的机制性根因，不是采集器偶发问题。

### 新增：db 层写库口统一清洗（所有采集器调用方受保护）

| 函数 | 职责 | 踩坑记录 |
|---|---|---|
| `sanitizeJobText` | 去 PUA 字形（U+E000–U+F8FF）→ 折叠空白 → 剥离尾部薪资残片 | — |
| `sanitizeSalary` | 薪资专用 | **必须独立实现**：复用 `sanitizeJobText` 会把合法薪资 `10-20k` 整串清成 `null`（实测踩到，dry-run 发现） |
| `stripCardTail` / `sanitizePosition` | 去掉卡片尾巴（`职位 5-9千 昆明·呈贡区 本科 java`） | 仅在能识别标记且截断后长度合理时生效，避免误伤「Java开发工程师·远程」 |
| `sanitizeCompany` | 剥「公司全称：」前缀 + 过滤 `APP下载/首页/登录` | — |

`upsertJob` 与 `updateJob` 均已接入；`JobRow` 接口同步补齐（上一轮正是漏了它导致构建长期是坏的）。

### P1-2 JD 回填：工具化 + 四平台选择器实测校准

新增 `scripts/backfill_jd.ts`（可续跑 / 限数量 / 失败不中断）与 `scripts/calibrate_jd_selectors.ts`（探测校准）：

| 平台 | JD 选择器（实测） | 实测长度 |
|---|---|---|
| boss | `.job-sec-text` | 383 字 |
| job51 | `.bmsg.job_msg` | 856 字 |
| zhilian | `[class*=detail-content]` | 529 字 |
| liepin | `[class*=job-intro-container]` / `.paragraph` | 1163 字 |

顺带补公司名（boss `.company-name`、51job `.cname`、智联 `.company__title`）。

**实测：boss 13/14 成功（170–507 字）；job51 4/6 成功（621–856 字）。**

### ⚠️ 修复中发现：51job 会整页替换成「滑动验证页面」

`--source=job51 --include-applied` 首次跑出 13/20 失败，日志显示「可能岗位已下架」——**这个判断是错的**。

```
dump 页面真相：
  title  = "滑动验证页面"
  正文    = 117 字「访问验证 别离开…请按住滑块，拖动到最右边」
  → 所有 JD / 公司选择器必然命中 null
```

不是数据丢失，是**触发滑块风控**（与投递时遇到的「访问验证」同一机制）。

问题不止于文案：**早期实现会把这批继续刷完 —— 无意义且加重风控**。

已修：页内识别风控（标题/正文匹配「访问验证｜滑动验证｜安全验证｜人机验证｜请按住滑块」）→
**命中即中止整批** + 输出处置建议；并把失败细分为「风控页 / 页面空白(<400字，疑似下架或需登录) / 选择器未命中」。
实测 job51 第 1 条即命中并中止（原来会空刷 20 次）。

### P2-4 数据修复：五个步骤 + 一个关键判断

新增 `scripts/fix_job_data.ts`（**默认 dry-run**，`--apply` 才写库且**自动备份** `data/chat.db.bak-<ts>`）：

1. 文本字段清洗
2. 按 `apply_url ↔ applications.job_url` 反查回填公司名（**60 条**，无需爬页面）
3. 同（公司+职位）组去重
4. 空直链的 candidate 标记 unavailable
5. **同投递入口去重**

**⑤ 的关键判断：不能一刀切。** offerbiu 的 `apply_url` 常是**公司级投递入口**（如 `career.huawei.com`），
多个不同部门岗位共用它 —— 那不是重复。判据 = 组内 **position 是否唯一**：

```
149 个 URL 组 → 43 组是共用入口（保留） ／ 106 条是真重复（合并删除）
```

**③ 的顺序陷阱**：重复组必须基于「清洗 + 回填**之后**的投影值」计算。用原始值算会漏掉「清洗后才变成同名」的组（实测 11 → 63）。

### 数据修复结果

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 岗位总数 | 1687 | 1545 |
| 含换行 `position` | 59 | **0** |
| `company` 为空 | 154 | **114** |
| 空直链的候选岗位 | 35 | **0** |
| 真重复（同源+同链+同职） | — | **0** |
| 无 JD 岗位 | 685 | **557** |
| JD 覆盖率 | 59% | **64%** |
| 高匹配中真正基于 JD | 58 | **63** |

> **投递历史未受影响**：`applications` 表 **691 条保持不变**（boss 366 / zhilian 117 / offerbiu 77 / liepin 71 / job51 60）。
> `jobs` 表 applied 计数下降只是去重了重复行，投递事实源头完好。

### P2-2 依赖漏洞

```bash
npm audit fix --registry=https://registry.npmjs.org   # npmmirror 不支持 audit 端点
```

**9 个（5 high / 4 moderate）→ 1 个（uuid moderate）**；`package.json` 版本范围**未变**，只更新了 lock 文件。

- 环境坑：npm 的 trash 删除被沙箱拦截（报 `safe-delete`）→ 先 `rm -rf node_modules/@types/nodemailer`
  （孤儿包，类型由 `server/shims.ts` 提供）后重试即成功。
- 剩余 `uuid`：本仓库只用 `v4()`，**不触发**该漏洞（漏洞在 v3/v5/v6 传 buf）；升到 `uuid@14` 属破坏性变更，暂不动。
- 副作用：React 开发界面 bundle 2.72MB → 3.91MB（新增依赖版本更大）；生产 `console.html` 不走打包器，不受影响。

### P2-3 验证流程（根治「构建坏了没人知道」）

```bash
npm run verify         # = tsc -b（含 server/）+ console:check
npm run selftest       # 51 项功能回归（只读）
npm run hooks:install  # 启用 .githooks/pre-push
```

- 新增 `.githooks/pre-push`：推送前自动跑 `tsc -b` + 控制台内联 JS 语法检查，不通过则阻断推送（可 `--no-verify` 跳过）。
- 本仓库 `tsconfig.node.json` 会把 `server/` 一并类型检查 —— 而服务端用 `tsx` 直跑 `.ts` 不做类型检查，
  所以 **`tsc -b` 是唯一的静态防线**，这就是 pre-push 存在的理由（已实测生效）。
- `selftest.ts` 从 32 项扩到 **51 项**：新增字段清洗 10 项 / 卡片尾巴 6 项 / 公司名 4 项；
  并把「AI 已启用」从断言改为提示，换台没配 LLM 的机器也能跑通。

---

## 三、残留与下一步

| 事项 | 说明 |
|---|---|
| **557 个岗位仍无 JD** | boss 268 / job51 91 / zhilian 127 / liepin 71。boss 可继续跑 `backfill_jd.ts --limit=50`；job51 需先人工过滑块；zhilian/liepin 需加 `--include-applied` |
| **114 个 company 仍为空** | 51job 详情页公司选择器未能校准（探测直接撞风控页），待人工过滑块后再补 |
| 51job 风控 | 该平台的采集/回填/投递都会被滑块拦，属平台侧限制，非代码缺陷 |
| React 开发界面 bundle 3.91MB | 未做代码分割（P3，生产不受影响） |

---

## 四、本次新增/修改文件

**新增**
- `scripts/fix_job_data.ts` — 历史脏数据修复（dry-run 默认 + 自动备份）
- `scripts/backfill_jd.ts` — JD/公司名回填（可续跑 + 风控识别）
- `scripts/calibrate_jd_selectors.ts` — 详情页选择器探测校准
- `.githooks/pre-push` — 推送前静态防线
- `FIXES_2026-09-19.md`（本文件）

**修改**
- `server/db.ts` — `sanitizeJobText` / `sanitizeSalary` / `stripCardTail` / `sanitizePosition` / `sanitizeCompany`；
  修 `upsertJob` 去重键 NULL 陷阱；`upsertJob`/`updateJob` 接入清洗
- `scripts/collect_multi.ts` — BOSS 选择器修正 + 无直链护栏
- `scripts/collect_boss.ts` — 无直链护栏
- `scripts/collect_51job.ts` — 职位名改用 sensorsdata（修 118 条污染源）
- `scripts/selftest.ts` — 扩到 51 项
- `package.json` / `package-lock.json` — npm scripts + 依赖安全升级
- `README.md` — 脚本速查、`npm run verify` 与 hooks 说明
- `.gitignore` — 忽略 `_probe_*.ts` / `_tmp_*.ts` 临时探针
- 删除遗留临时探针 `scripts/_probe_pos.ts`（无引用、含硬编码人名过滤）
