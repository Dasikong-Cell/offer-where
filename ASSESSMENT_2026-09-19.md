# offer-where 软件测评报告

测评日期：2026-09-19 ｜ 版本：commit `b3d65a8` ｜ 测评方式：**只读实测**（不发信、不投递、不改档案）

---

## 一、结论

**总评 7.6 / 10（修前 6.9）** —— 这是一套**功能完整度与工程纪律都明显高于平均水平**的个人向求职自动化系统：
53 个 API、5 平台 + 邮箱直投 + 自动回复 + 匹配/定制 + 数据看板，README 与踩坑沉淀相当扎实。

但测评暴露了 **3 类结构性问题**，其中 1 类已在本次测评中修复：

| 类别 | 严重度 | 状态 |
|---|---|---|
| 构建类型检查失败（`tsc -b` 报错，构建一直是坏的） | 🔴 高 | ✅ 已修 |
| 简历解析抽不到带分隔符的手机号（`130-9532-8850` 是简历常见写法） | 🔴 高 | ✅ 已修 |
| 登录态检测**双向误报**（把未登录的猎聘判成已登录） | 🔴 高 | ✅ 已修 |
| 匹配度看板**误导性展示**：高匹配 316 个里 242 个是「仅职位名兜底」出来的 | 🟠 中 | ✅ 已修（如实标注） |
| 685 / 1687 岗位无 JD；BOSS 岗位 `company` 全空致 150 条记录不一致 | 🟠 中 | ⏳ 待修（需改采集器） |

---

## 二、评分表

| 维度 | 权重 | 修前 | 修后 | 评语 |
|---|---|---|---|---|
| 功能完整度 | 15% | 9.0 | 9.0 | 对标矩阵全绿，53 个 API 覆盖投/跟/采/配/定制 |
| 核心链路可用性 | 20% | 6.5 | 7.0 | BOSS 全自动顺畅（366 投递）；猎聘/51job 反爬需人工过验证 |
| **数据质量与匹配可信度** | 20% | 4.0 | 5.0 | ⚠️ 最大短板：40.6% 岗位无 JD，匹配分大面积为兜底值 |
| 工程质量（构建/依赖/测试） | 15% | 5.5 | 8.0 | 构建曾坏、9 个依赖漏洞、零测试 → 已修构建、补 32 项自检 |
| 健壮性与降级 | 15% | 8.5 | 9.0 | LLM 软失败回退、Cookie 弱信号 + 页面权威双轨、隔离闸门 |
| 可运维性 | 10% | 8.5 | 8.5 | 一键启动器、体检/自检脚本、记忆与技能沉淀完备 |
| 文档与知识沉淀 | 5% | 9.0 | 9.0 | README + 对标矩阵 + 方案文档 + 2 个可复用技能 |
| **加权总分** | | **6.9** | **7.6** | |

---

## 三、实测证据

### 3.1 环境（全绿）

```
后端      {"status":"ok","ai":true}
AI        {"enabled":true,"model":"deepseek-chat","baseUrl":"https://api.deepseek.com/v1"}
CDP 连接   boss=9223 ✅  liepin=9224 ✅  job51=9225 ✅  zhilian=9226 ✅  offerbiu=9227 ✅  (5/5, Chrome 151)
```

### 3.2 数据层

```
表行数        jobs 1687 ｜ applications 691 ｜ hr_conversations 57 ｜ form_memory 0
岗位池        offerbiu 966(投76)  boss 373(投354)  job51 149(投42)  zhilian 128(投117)  liepin 71(投71)
近 7 天投递    09-18:59  09-17:28  09-16:33  09-15:119  09-14:20  09-12:94  09-11:90
匹配分覆盖     1687/1687 = 100%，均值 16.6
HR 会话       boss active 39 / done 11 / replied 1；liepin active 6；AI 回复 38 条
DB 体积       1.5 MB（WAL 3.9 MB）
```

### 3.3 匹配可信度（本次最关键发现）

```
岗位池 × 匹配分档：
  无 JD：高(≥70) 258 ｜ 中 3 ｜ 低 424          ← 高分几乎全在「无 JD」这一侧
  有 JD：高 0      ｜ 中 58 ｜ 低 944

BOSS 高分行（≥70）共 246 个 → 其中 242 个（98.4%）是「无 JD、仅职位名兜底」
真实基于 JD 的高匹配：全池仅 58 个（其中 BOSS 4 个、offerbiu 54 个）

有 JD 岗位的平均分：boss 48.4(n=35) ｜ offerbiu 6.9(n=966) ｜ zhilian 42(n=1)
```

**结论**：`matchResumeToJob` 在无 JD 时会用职位名兜底并**封顶 70 分**（设计正确），但这导致
**看板上「高匹配 ≥70」的 316 个里只有 58 个是真的**。用户若按匹配度排序投递，实际是在按「职位名关键词重合」排序。

### 3.4 数据质量

```
无 JD 岗位            685 / 1687（40.6%）   boss 338  job51 149  zhilian 127  liepin 71
BOSS position 含换行   59 / 373（15.8%）     如 "Java\n-K"、"java开发工程师\nK"（薪资残片）
空 apply_url          35
重复岗位(同源+公司+职位) 11
applied 但无投递记录    150  ← 全部是 BOSS，且 company 均为 null
applications 中 company 为空 122
quarantine 标记       0（只在新扫描时写入）
```

**150 条不一致的根因**：BOSS 采集器抓不到公司名（`.boss-name` 选择器失效）→ `jobs.company = null` →
`jobs.status='applied'` 事后按 `company + position` 反查 `applications` 表自然匹配不上。
同一根因还会削弱自动回复的「按公司名反查 HR 职位」兜底链（现靠 DOM `.position-name` 顶住）。

### 3.5 功能实测（`scripts/selftest.ts`，32/32 通过）

```
A  简历解析      PDF 4150 字符 → 姓名/手机/邮箱/技能 26 项/searchBlob 3003 字符  ✅
A2 手机号多格式   130-9532-8850 → 13095328850 ｜ 138 0013 8000 ｜ +86 187 0000 0001  ✅
B  匹配引擎      高相关92分 / 低相关0分 / 可复现 / 无JD兜底封顶70 / 全空不崩  ✅
C  邮箱抽取      正常抽取 ✅ ｜ noreply@ 与 w3.org 正确过滤 ✅ ｜ 域名归约 com.cn/co.uk ✅
D  一岗一简历     LLM 生成 79 分、技能按 JD 重排、防幻觉（未凭空新增技能）✅
```

### 3.6 工程质量

```
构建      tsc -b + vite build            修复前：3 个 TS2344/TS2339 报错，构建失败
        → 修复后：通过 ✅  dist 2.72 MB（gzip 763 KB，单 chunk 超 500KB 警告）
依赖      17 deps + 16 devDeps ｜ npm audit: 9 漏洞（5 high / 4 moderate / 0 critical）
控制台     public/console.html 内联 JS 33,664 字符 语法校验通过 ✅
仓库      .git 仅 1.9 MB；.gitignore 详尽（data/ node/ .env 均正确忽略）
源码      23,694 行（ts/tsx/html）
```

**依赖漏洞的真实影响**：
- `qs` / `express` / `body-parser`（moderate，DoS）→ **在生产路径**（Express 后端），本体单机自用风险低，但 `npm audit fix` 可无破坏修复；
- `mermaid` / `cherry-markdown` / `dompurify` / `tdesign-*`（high）→ 全部是 **`src/` React 开发界面**的依赖，生产控制台是手写 `console.html`（无打包器）→ **生产暴露面为零**；
- `uuid`（moderate）→ 仅需破坏性升级；本项目只用 `v4()`，**不触发**该漏洞（v3/v5/v6 且传 buf）。

### 3.7 登录态实测（修复后）

```
✅ boss     已登录   标记 ["消息","简历"]          页面含「杨欣宇」
✅ job51    已登录   标记 ["我的求职","在线简历"]   页面含「Hi，杨欣宇」
❌ liepin   未登录   标记 ["登录/注册","密码登录","获取验证码"]
✅ zhilian  已登录   标记 ["个人中心"]             页面含「杨欣宇」「消息 6」
```

---

## 四、本次修复的 4 个缺陷

### ① 构建一直是坏的（P1）
`tsc -b` 报 `TS2344`：`"quarantine" does not satisfy the constraint 'keyof JobRow'`。
根因：上一轮加列时改了 schema / 迁移 / `updateJob` 的 Pick 联合类型，**漏改 `JobRow` 接口**。
因为服务端用 `tsx` 直跑 `.ts`（**不做类型检查**），运行时毫无异常 → 缺陷被完全掩盖。
→ 修：`JobRow` 补 `quarantine: string | null`。

### ② 简历解析抽不到手机号（P1）
`PHONE_RE = /1[3-9]\d{9}/` 要求 **11 位连续数字**，而简历里写的是 `电话：130-9532-8850`（带连字符）。
→ 修：正则允许 `-`/空格分隔 `1[3-9]\d(?:[-\s]?\d){8}`，命中后 `normalizePhone()` 归一化（含去 `+86`）。
→ 补 5 个格式回归用例（分段/空格/连续/国际区号/无号）。

### ③ 登录态检测双向误报（P1）
两处同类缺陷：
- `scripts/check_logins.ts`：**只打印标记数组不给结论**；且 liepin 的 `logged` 标记含泛词「简历」
  （命中未登录首页的「简历优化」），`anon` 标记写的是「请登录/账号登录」而真实页面是
  「登录/注册/密码登录/获取验证码」→ **一个都不命中**，双向失准，把未登录判成已登录。
- `platformApi.isLoggedIn()`：按 Cookie 名判定，但猎聘未登录访客同样带 `XSRF-TOKEN`/`__gc_id`/`fe_se`。

→ 修：
  - `check_logins.ts` 重写为 **anon 优先**规则 + 明确结论 + 退出码（0/1/2）+ 补 zhilian 平台；
  - `isLoggedIn` 降级为 **cookieHint（弱信号）**并附说明，新增 **`verifyLoginViaPage()`** 页面级权威判定；
  - `/api/platform-api/probe?deep=1` 同时返回两者。
→ 实测对照：猎聘 `cookieHint:true` vs `pageVerdict:"not-logged-in"` —— 弱信号误报被权威判定纠正。

### ④ 匹配度看板误导性展示（P2，我上一轮引入）
看板只报「高 316 / 中 / 低」，不区分分数是「基于 JD」还是「仅职位名兜底」，
让用户以为有 316 个高匹配岗位。→ 修：funnel API 增加 `titleOnly` / `jdBased` / `highWithJd`
（**无需改表结构**，由 `jd` 是否为空推导），控制台加黄色可信度提示行。
→ 现在如实显示：「高匹配 316 个里真正基于 JD 的只有 58 个」。

---

## 五、未修问题（按优先级）

### P1 — BOSS 采集器抓不到公司名
- 影响：150 条投递记录不一致、122 条 applications 无公司、自动回复公司反查失效、按公司统计不可用。
- 建议：采集时同时读 `.job-card-wrap` 内的公司链接文本与 BOSS 详情页 `brandName`；
  写库前做「company 为空则跳过入库或回填」的护栏；并写一次性回填脚本修历史数据。

### P1 — 685 个岗位缺 JD（40.6%）
- 影响：匹配分不可信、一岗一简历无法按真实 JD 定制、按匹配度排序投递是无效优化。
- 建议：在 `collect_boss.ts` / `collect_job51` / `collect_zhilian` 的详情页阶段抓取 JD 正文；
  采集器加「JD 为空的岗位打 `jd_missing` 标记」，看板把这类岗位单列，不与已评分岗位混算。

### P2 — BOSS `position` 字段脏（59/373 含换行与薪资残片）
- 例：`"Java\n-K"`、`"java开发工程师\nK"`。
- 影响：展示脏、`criteria.keywords` 职位过滤可能失效。
- 建议：入库前统一清洗 `position.replace(/\s+/g,' ').replace(/[-–]?\d*[Kk千]\b/g,'').trim()`。

### P2 — 依赖漏洞 9 个
- 建议：`npm audit fix`（8 个非破坏性可修）；`uuid` 保持现状（本仓库不触发）；
  `mermaid/cherry-markdown/dompurify/tdesign` 属开发界面，可随手升级或移入 `devDependencies`。

### P2 — 零自动化测试（本次已补 32 项）
- 建议：把 `scripts/selftest.ts` 接进 `npm run build` 之后或 git pre-push，防回归。
  特别是**服务端走 tsx 不做类型检查**，所以「构建」是唯一的静态防线，必须有人跑。

### P2 — 11 个重复岗位 / 35 个空 `apply_url`
- 建议：采集入库统一走 `upsertJob`（已有去重逻辑）；`apply_url` 为空的岗位在采集阶段丢弃。

### P3 — 开发界面 bundle 2.72 MB（gzip 763 KB，单 chunk 超限）
- 生产不走这条路径，影响有限；若要优化可对 mermaid/tdesign 做 `manualChunks` 或动态 import。

---

## 六、测评方法与局限（诚实说明）

**做了什么**
- 只读实测：环境/连接/AI、DB 全量统计与数据质量、32 项功能回归、构建与依赖审计、4 平台登录态实测。
- 修复后逐项复验（构建通过、自检 32/32、隔离闸门 PASS、控制台语法 OK）。
- 所有脚本均标注为「只读」，新增的 `db_report.ts` 以 `readonly: true` 打开数据库。

**没做什么（局限）**
1. **未做真实投递**（简历不可撤回）——投递链路的可用性结论来自历史数据（691 条记录）与既往实测，
   非本次现场复跑；本次只对隔离闸门做了合成数据的端到端自检。
2. **未重跑 offerbiu 全池扫描**（会触发微信限流、且耗时长）——邮箱命中率沿用历史结论（~858 岗命中 ~69）。
3. **未验证逆向检索接口**（`bossSearchJobs`/`liepinSearchJobs` 默认关闭，端点字段待实测校准）。
4. 自动回复仅审计了落库分布（57 条会话 / 38 条 AI 回复），未做真实发送验证。
