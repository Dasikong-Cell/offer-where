# 平台登录指南

> 本工具采用「职得鸭模式」：**官网一键投递，不代填表单、不碰级联选择**。
> 因此它完全依赖**你账号里已填好的在线简历** —— 简历不完整会被站点拦截。

---

## 一、为什么要先登录

投递是**用你自己的真实 Chrome**去点「申请」按钮（CDP 接管调试窗口），所以：

- 登录态由 Chrome 的 `--user-data-dir` 持久化在磁盘上，**登录一次即可长期复用**；
- 工具**不代管账号密码**，也不会替你注册；
- 未登录时平台不会报错说「请登录」，而是**静默返回 0 个岗位** —— 这是最容易误判的坑。

---

## 二、首次使用：逐个平台登录

双击 `start_all.bat`（或桌面「投递Agent」）后，会为每个平台打开**独立**的 Chrome 窗口。
在对应窗口里完成登录即可：

| 平台 | key | 登录地址 | 备注 |
|---|---|---|---|
| BOSS直聘 | `boss` | https://www.zhipin.com/web/user/?ka=header-login | 反爬最强，**建议登录后先手动浏览几个页面**再交给自动化 |
| 前程无忧 | `job51` | https://login.51job.com/login.php | 可能弹**滑块验证**，需人工过一下 |
| 猎聘 | `liepin` | https://passport.liepin.com/ | 连续快速访问详情页会触发**短信验证**，建议放慢节奏 |
| 智联招聘 | `zhilian` | https://www.zhaopin.com/ | — |
| 牛客网 | `nowcoder` | https://www.nowcoder.com/login | — |
| 其余已登记平台 | 见控制台「平台通道」 | 在各平台官网登录 | 用 `start_platforms.bat <平台>` 打开对应窗口 |

**其余 8 个平台的登录地址**可直接在控制台「平台通道」卡片点「打开窗口」跳转，无需手记 URL。

---

## 三、登录是否真的成功：看「收集到几个岗位」

⚠️ **不要只看控制台显示的「已登录」**。历史上多次出现：检测器报「已登录」，但搜索页**收集到 0 个岗位**、页面对自动化浏览器返回空白 —— 实际并未登录。

**判定标准（两个都满足才算就绪）**：

1. 巡检状态为 `logged` / `已登录`
2. 同平台搜索**能收集到 N 个岗位（N > 0）**

控制台首页「开箱自检」面板 + 「平台通道」巡检可复核这两项。

---

## 四、登录后：补全在线简历

在目标平台把**在线简历**填写完整（本工具不代填表单）。建议至少包含：
工作/项目经历、教育经历、期望职位与城市、联系方式。

---

## 五、开始投递

控制台 →「投递中心」→ 勾选平台 → 设置数量与间隔 → **先点「仅预览」**确认链路 → 再真实投递。

安全阀：

- 每日上限默认 **40**（按平台计，可用 `APPLY_DAILY_LIMIT` 或控制台输入框调整）
- 命中平台风控（额度到顶 / 账号异常）会**自动封锁**该平台，控制台可查看剩余时间并提前解封
- 浏览器默认**可见窗口**，遇验证码/滑块在窗口内人工过一下即可

---

## 六、服务端口

后端监听 **`http://127.0.0.1:4400`**（`PORT` 环境变量可改，但控制台与投递/采集脚本都按 4400 约定，
改端口需同步改脚本，否则控制台连不上）。

启动方式：

- 一键：双击 `start_all.bat`（自动开窗口 + 起后端 + 打开控制台）
- 只起后端：`start_server.bat`
- 只开某几个平台窗口：`start_platforms.bat boss liepin`（`all` = 全部已登记平台）
