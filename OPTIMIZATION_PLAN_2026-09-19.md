# 优化方案（基于第二轮复测）

> 复测结论：**功能面已对齐职得鸭并反超，瓶颈 100% 在数据** —— 真 JD 只占 26%。
> 本方案只解决这一件事。**在真 JD 覆盖率到 50% 之前，不建议再加新功能。**

---

## 一、现状与目标

| 指标 | 当前 | 目标 | 说明 |
|---|---:|---:|---|
| 真 JD 覆盖率 | **26%**（405/1545） | **≥50%** | 唯一北极星指标 |
| 卡片摘要被当成 JD | **964** | **0** | 它会让匹配/求职信/定制简历全部失效 |
| 无 JD 岗位匹配分虚高 | 有（职位名兜底封顶 70） | 0 | 虚高分会让"按匹配度排序投递"变成随机排序 |
| offerbiu 可用岗位 | **0 / 963** | ≥271 | 占池 62% 的通道目前完全用不了 AI 能力 |

---

## 二、可回填潜力（已实测域名分布）

offerbiu 963 个 `apply_url` 的域名分布，决定了哪些能救：

| 来源 | 数量 | 能否抓到真 JD | 难度 | 优先级 |
|---|---:|---|---|---|
| `mp.weixin.qq.com` 微信推文 | **271** | ✅ **正文里就有岗位描述** | 低（**已有能力**） | **P0** |
| `app.mokahr.com` Moka ATS | 86 | ✅ 通常有 JD 详情页 | 中 | P1 |
| `wecruit.hotjob.cn` 北森 | 30 | ✅ | 中 | P1 |
| 智联/51job 校招系 | 47 | ✅ | 中 | P1 |
| `www.iguopin.com` 国聘等 | 12 | ✅ | 中 | P2 |
| 企业自有门户（华为/中石油/移动…） | ~50 | ⚠️ 结构各异 | 高 | P2 |
| `v.wjx.cn` 问卷星等纯表单 | 8+ | ❌ **永远没有 JD** | — | 直接标记不可用 |

**结论：微信推文 271 个是最大且最容易的一块**（占 offerbiu 的 28%），
而且我们**已经有能力** —— 之前扫 HR 邮箱时就是在打开推文抽正文，复用即可。

加上其他平台的自动回填（boss 38 / liepin 10 / zhilian 52）与 job51（76，需你先过滑块）：

```
当前真 JD        405  (26%)
+ 微信推文       271
+ boss/liepin/智联 100
+ job51(人工后)   76
──────────────────────
可达             852  (55%)
```

---

## 三、执行方案

### 阶段 0：止损（0.5 天 · 零风险 · 建议立刻做）

这一步**不增加任何 JD**，但能立刻停止"假 JD 污染 AI 功能"，是后续一切的前提。

| # | 动作 | 说明 |
|---|---|---|
| 0.1 | `jobs` 加列 `card_text`，把 964 条卡片摘要**从 `jd` 迁出**，`jd` 置空 | `company/position/city` 已独立成列（963/963/869 已填充），迁出不会丢信息 |
| 0.2 | 匹配引擎：无 JD 时**不再用职位名兜底打分** | 现在 `matchResumeToJob` 无 JD 会用职位名兜底并封顶 70 → 这就是"BOSS 246 个高分里 242 个是兜底"的根因。改为 `null` + 标注 `basis:'none'` |
| 0.3 | funnel / 控制台改为**三分口径** | 真 JD / 卡片摘要 / 无 JD，不再报一个笼统的"覆盖率" |
| 0.4 | 纯表单入口标记不可用 | `v.wjx.cn` 等 8+ 个，直接置 `jd_unavailable`，不再尝试回填 |
| 0.5 | 采集器入口治本 | `offerbiuCollect` 不再把卡片摘要写进 `jd`（**防止复发**，否则回填完还会再被覆盖） |

**验收**：`db_report` 中「疑似卡片摘要」= 0；funnel 中 `titleOnly` 归零；控制台显示三分口径。

---

### 阶段 1：微信推文回填（1 天 · +271 · 最高性价比）

新增 `scripts/backfill_wechat_jd.ts`：

```
取 offerbiu 中 apply_url 为 mp.weixin.qq.com 且 jd 为空的岗位
→ 复用现有"打开推文"能力（offerbiuEmailScan 已在用）
→ 提取正文（剥 svg / 去导航词 / 截断 4000 字）
→ 写入 jobs.jd
→ 分批 + 间隔 + 遇风控立即中止（backfill_jd.ts 已有该机制，直接复用）
```

**风险与对策**：

| 风险 | 对策 |
|---|---|
| 微信批量访问会限流（已实测过） | 分批 30~50 个/轮，间隔 1.5s，命中风控页立即停并提示冷却 |
| 推文可能是"招聘汇总"而非单岗位 JD | 回填后按长度与关键词抽样人工核验 10 条，再决定放量 |
| 推文正文含大量无关内容（公司介绍/投递方式） | 截断 + 交给 AI 前先做一次"是否含岗位职责"的轻量判定 |

**验收**：offerbiu 真 JD ≥ 200（考虑到限流与部分推文无实质内容，不苛求 271 全中）。

---

### 阶段 2：ATS / 企业门户回填（2~3 天 · +213 · 视阶段 1 效果决定）

按域名分组、逐个校准选择器，复用已有的 `scripts/calibrate_jd_selectors.ts`：

1. `app.mokahr.com`（86）—— 结构统一，性价比最高
2. `wecruit.hotjob.cn`（30）
3. 智联/51job 校招系（47）
4. `www.iguopin.com` 国聘等（12）
5. 企业自有门户（~50）—— 结构各异，**最后做**，遇到需要登录的直接放弃

**原则**：每类域名校准 1~2 个样本即可，命中率 <50% 的类直接放弃，不硬啃。

---

### 阶段 3：其他平台剩余（0.5 天 · 需你配合一次）

| 平台 | 数量 | 做法 |
|---|---:|---|
| boss | 38 | 直接跑 `backfill_jd.ts --source=boss` |
| liepin | 10 | 同上 |
| zhilian | 52 | 同上 |
| **job51** | **76** | ⚠️ **需要你先在对应 Chrome 窗口人工过一次滑块**，冷却后再跑 |

---

### 阶段 4：质量闭环（持续，防止复发）

- `selftest` 加断言：**卡片摘要不得写入 `jd`**（把 0.1/0.5 的修复固化成回归）
- `db_report` 已加「疑似卡片摘要」指标
- 控制台「目标岗位」下拉已加 `isCardSummary()` 过滤
- 每次批量回填后跑一次 `db_report` 核对三分口径

---

## 四、执行顺序建议

```
立刻做  阶段 0（止损，0.5 天，零风险）
  ↓
接着做  阶段 1（微信推文 271，最高性价比）
  ↓
看效果  阶段 3 的 boss/liepin/智联（100，顺手做完）
  ↓
再评估  阶段 2（ATS 213，投入产出比递减，可只做 mokahr 那 86 个）
  ↓
需你配合 job51 76（过滑块后一次跑完）
```

**如果只做一件事**：做阶段 0 + 阶段 1。真 JD 从 26% → 约 44%，且所有 AI 功能不再产出垃圾。

---

## 五、非 JD 相关的遗留（JD 达标后再说）

| 项 | 说明 | 优先级 |
|---|---|---|
| 16 条已投但无投递记录 | 智联 10 条集中同一公司 → 批量投递应逐条落 `applications` | P1 |
| PNG 简历发送未在真实聊天验证 | 生成侧已通，**发送侧需你手动试一次**才能放量 | P1 |
| 打招呼文案未接平台发送 | `bossChat`/`liepinChat` 已有发送能力，可补 | P2 |
| 前端 bundle 4.4MB | vendor 1.7MB + markdown 1.5MB；**生产 `console.html` 不走打包器，不影响生产** | P2 |
| `uuid` moderate | 仅 v3/v5/v6 传 buf 触发，本项目只用 `v4()`，不可利用 | P3 |

---

## 六、需要你决定的两件事

1. **是否现在就执行阶段 0 + 阶段 1？** 阶段 0 是纯止损、零风险，我可以直接做；阶段 1 会占用 offerbiu 的 Chrome 窗口约 30~60 分钟。
2. **job51 的 76 个岗位要不要救？** 需要你在 51job 那个 Chrome 窗口里**手动过一次滑块**，之后我才能跑。不过滑块（你不用管的那部分我可以等）。

---

*依据：`ASSESSMENT_2026-09-19-v2.md`（第二轮复测，总评 8.4）*
*数据：1545 岗位 / 真 JD 405 / 卡片摘要 964 / 微信推文入口 271*
