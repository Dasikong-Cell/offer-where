# job-apply-agent 复测报告（第二轮）

> 时间：2026-09-19 12:36 ｜ 方法：**只读实测**（不发信、不投递、不改档案）
> 与首轮（09-19 02:00，总评 7.6）对照，本轮重点是验证「补齐职得鸭功能 + 修技术债」后的真实收益，
> 以及**新引入的东西是否带来了新问题**。

---

## 一、总评：8.4 / 10（首轮 7.6）

| 维度 | 首轮 | 本轮 | 变化依据 |
|---|---:|---:|---|
| 功能完整度（对标职得鸭） | 9.0 | **9.5** | 求职信/面试攻略/简历版本/时间片/猎聘交换/岗位定位全部补齐，且**多出双通道** |
| 核心链路可用性 | 7.0 | **7.5** | 闭环完整、可无人值守；但 JD 质量仍限制 AI 能力发挥 |
| **数据质量与匹配可信度** | 5.0 | **5.5** | 仍是最大短板：真 JD 只有 26%，且**首轮的覆盖率口径是错的** |
| 工程质量 | 8.0 | **8.5** | tsc / 58 项自检 / pre-push 钩子 / 依赖仅剩 1 个不可利用 |
| 健壮性与降级 | 9.0 | **9.0** | safeOp 已接线，关闭态错误不再污染失败率 |
| 可运维性 / 可观测性 | 8.5 | **9.0** | 新增反检测自检、平台巡检；db_report 口径已修正 |
| 文档 | 9.0 | **9.0** | 三份报告 + 竞品深度解析 |
| **反检测（本轮新增维度）** | — | **9.0** | **可实测**且 0 BAD / 0 WARN |

---

## 二、⚠️ 本轮最重要的发现：JD 覆盖率是虚高的

首轮报告写的是「JD 覆盖率 64%→74%→89%」。**这个数字不成立。**

抽查 offerbiu 岗位的 `jd` 字段，实际内容是：

```
中大咨询集团 更新 9月2日 博士顾问、顾问培训生、顾问培训生FDE培养方向
政府/事业单位/社会组织 民企 北京、广州、香港 2027 届 尽快投递 秋招 需要笔试 投递入口
```

这是**列表页卡片的 innerText**，不是岗位描述。它通过了所有"JD 非空"的检查，
但对匹配、求职信、定制简历、面试攻略**全部无效**。

### 真实口径

| 平台 | 总数 | 无 JD | 卡片摘要 | **真 JD** | 其中 ≥200 字 |
|---|---:|---:|---:|---:|---:|
| boss | 282 | 38 | 1 | **243** | 189 |
| liepin | 71 | 10 | 0 | **61** | 61 |
| job51 | 101 | 76 | 0 | **25** | 25 |
| zhilian | 128 | 52 | 0 | **76** | 71 |
| offerbiu | 963 | 0 | **963** | **0** | 0 |
| **全池** | **1545** | 176 | **964** | **405（26%）** | **346（22%）** |

**结论**：占岗位池 62% 的 offerbiu 通道，**零个真 JD**。
→ 匹配分在 offerbiu 上全无意义（首轮也观察到"966 个有 JD 岗位平均分仅 6.9"，当时归因错了，真因就是这个）。
→ 昨天新建的求职信 / 定制简历 / 面试攻略，对 offerbiu 岗位产出的是垃圾。

### 已修：我自己引入的连带 bug

昨天写的控制台「目标岗位」下拉用 `jd.length > 50` 过滤 —— 卡片摘要有 80~120 字，**会被当作有 JD 列出来**。
已修：新增 `isCardSummary()` 识别（含"投递入口"/"尽快投递"/"更新 X月X日"），从下拉里排除。
同步修了 `scripts/db_report.ts`：新增「疑似卡片摘要」指标，并把一致性校验改为正确口径（见下）。

---

## 三、第二个发现：「已投但无投递记录 122 条」是**度量 bug**，不是数据丢失

首轮把它列为 P1（归因于 `jobs.company` 为空）。本轮查清：

```
jobs.position        = '招聘Java开发工程师 Java Development Engineer'   ← 1 个空格
applications.position= '招聘Java开发工程师   Java Development Engineer' ← 3 个空格
```

关联用的是 `company + position` **精确文本比较**，采集路径与投递路径的空白折叠不一致就断链。

| 关联键 | 报告为"无记录"的条数 |
|---|---:|
| `company + position` 精确匹配 | **122**（首轮就是这么算的） |
| `apply_url ↔ job_url` | **16** |

**真实缺口只有 16 条**（智联 10 条是同一家公司的多个岗位，疑似批量投递未逐条落 `applications`）。
投递事实（`applications` 691 条）本身是完整的。

已修 `db_report.ts`：主指标改用 `job_url` 关联，文本匹配降为"参考"并标注原因。

---

## 四、功能覆盖度实测（对标职得鸭）

| 职得鸭功能 | 我们 | 实测状态 |
|---|:--:|---|
| AI 打招呼（type1） | ✅ | `greetDecision` 硬规则+AI，返回原因与证据 |
| AI 写求职信（type2） | ✅ | `coverLetter` LLM 真实生成 + 模板变量 + 三态去重 |
| AI 复聊（type3） | ✅ | `autoReply` 11 类 HR 意图 + LLM |
| AI 自动化（type4） | ✅ | `batch`/`engine` 编排，每岗回收标签 |
| 关键词打招呼（type5） | ✅ | 关键词筛选 + 排除词表 |
| 公司背调 / 搜索 | ✅ | `jobLocate` |
| 猎聘交换联系方式 | ✅ | 三态状态机（索要中/对方已发起/正常） |
| 简历双版本 | ✅ | 原始 / 优化后 / 一岗一简历 |
| 一岗一简历 | ✅ **更强** | **双通道**：有邮箱→PDF 邮件；无邮箱→PNG 聊天图 |
| 面试攻略 | ✅ | 岗位快照+考察重点+面试题，带防幻觉提示 |
| 时间段调度 | ✅ | 多时段 + 续跑 + 到点停 |
| 计费 / 套餐 / 分销 | ❌ **不做** | 定位差异，非缺陷 |

**我们独有的、它没有的**：邮箱直投通道、跨公司串号隔离、PDF 定制简历、反检测可实测。

---

## 五、工程质量与安全

| 项 | 结果 |
|---|---|
| `tsc -b` | ✅ 无错误 |
| `selftest` | ✅ **58 / 58** |
| 控制台内联 JS | ✅ 语法正确（43.6k 字符） |
| `vite build` | ✅ 41s，6 个 chunk（index 114KB / vendor 1.7MB / markdown 1.5MB / react 176KB / tdesign 306KB / icons 11KB） |
| `npm audit` | 1 个 moderate（`uuid`，仅 v3/v5/v6 传 buf 时触发，本项目只用 `v4()`，**不可利用**） |
| git | 工作区干净，`.git` 1.3MB |
| 端点 | 75 个 |
| **反检测实测** | ✅ **0 BAD / 0 WARN**：`navigator.webdriver=false`、plugins=5、window.chrome 存在、native toString 正常、WebGL 真实 |
| 平台连接 | 4/4 CDP 正常 |
| AI | deepseek-chat 已启用 |

> 反检测这一项要特别说明：首轮没有这个能力，**只能靠"我配了参数"来相信**。
> 竞品职得鸭的 4 项"卖点级"反检测全是误配未接线 —— 这正是"不实测就会翻车"的证明。
> 现在有了 `scripts/check_stealth.ts`，改启动参数或换 puppeteer 方案后可以立刻验证。

---

## 六、问题清单

### P0 — 阻塞（决定了 AI 能力能覆盖多少岗位）

1. **offerbiu 963 个岗位没有真 JD（占池 62%）**
   影响：匹配分无效、求职信/定制简历/面试攻略对该池全部不可用。
   可选处置：
   - A）给 `offerbiuCollect` 增加详情页抓取，回填真 JD（成本：需逐个访问，规模大）；
   - B）把卡片摘要从 `jd` 字段**清出去**（置空或另存 `card_text`），让所有下游逻辑诚实地区分"有 JD / 无 JD"；
   - C）对无 JD 的岗位，匹配分显式标注"不可信"，不参与排序。
   **建议先做 B+C（低成本、立刻止损），再做 A。**

### P1 — 明确缺陷

2. **16 条已投但无投递记录**（智联 10 条集中同一公司）→ 批量投递应逐条落 `applications`
3. **job51 无 JD 76/101** —— 滑块风控导致回填中止，需人工过验证后重跑
4. **一岗一简历的 PNG 发送未在真实聊天验证** —— 生成侧已通，发送侧（`sendChatResumeImage`）无人值守下没跑过，需你手动试一次再放量
5. **打招呼文案未接平台发送** —— `letterWriter` 已按冗余删除；`bossChat`/`liepinChat` 具备发送能力，可作为下一步

### P2 — 优化项

6. 重复岗位 10 个（同名不同直链，属预期，保持现状）
7. 前端 bundle 4.4MB（vendor 1.7MB + markdown 1.5MB 是大头；生产控制台 `console.html` 不走打包器，**不影响生产**）
8. `uuid` moderate 保留（升级为破坏性变更且不可利用）

---

## 七、修复项（本次测评过程中已修）

| 修复 | 说明 |
|---|---|
| 控制台「目标岗位」下拉 | 排除卡片摘要，避免给 offerbiu 岗位生成垃圾文案（**我自己上一轮引入的 bug**） |
| `db_report.ts` 一致性口径 | 关联键改用 `apply_url ↔ job_url`（122 → 16），文本匹配降为参考并注明原因 |
| `db_report.ts` 新增指标 | 「疑似卡片摘要（非真 JD）」，防止覆盖率再次虚高 |

---

## 八、结论

补齐职得鸭功能后，**功能面已经基本对齐，且在"一岗一简历双通道"上反超**（它只有 PNG 聊天图，我们 PDF 邮件 + PNG 聊天图都有）。
工程质量从 8.0 升到 8.5，反检测从"不可知"变成"可实测且全绿"。

**但真正的瓶颈没变，而且本轮才看清它的真面目**：不是"685 个岗位缺 JD"，
而是 **964 个岗位的 JD 是假的（卡片摘要）+ 176 个为空**，真正可用的 JD 只占 **26%**。
只要这个数字不解决，求职信、定制简历、面试攻略这些新功能就只能在 boss / 猎聘 / 智联这 25% 的池子里发挥作用。

**下一步的投入应该全部压在这一件事上**，而不是再加新功能。

---

*复现方式：`npm run selftest` ｜ `tsx scripts/db_report.ts` ｜ `tsx scripts/check_stealth.ts` ｜ `GET /api/stats/funnel` ｜ `GET /api/platforms/health?deep=1`*
